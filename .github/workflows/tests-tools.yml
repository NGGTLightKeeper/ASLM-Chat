# Copyright NGGT.LightKeeper and Di120078. All Rights Reserved.

name: Tests - Tools

on:
  push:
  pull_request:
  issues:
    types:
      - opened
      - reopened

concurrency:
  group: tests-tools-${{ github.ref }}
  cancel-in-progress: true

jobs:
  context_compression:
    name: context_compression
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Cache server venv
        uses: actions/cache@v5
        with:
          path: Data/venvs/server
          key: aslm-chat-venv-server-${{ runner.os }}-${{ hashFiles('Settings/venv_requirements.json') }}
          restore-keys: |
            aslm-chat-venv-server-${{ runner.os }}-

      - name: Prepare venv
        run: |
          python -c "from Services import venv_manager; import sys; sys.exit(0 if venv_manager.ensure_venv('server', log=True) else 1)"
          VENV_PYTHON=$(python -c "from Services import venv_manager; print(venv_manager.get_venv_python('server'))")
          echo "VENV_PYTHON=${VENV_PYTHON}" >> "$GITHUB_ENV"
          test -x "$VENV_PYTHON"

      - name: Install pytest
        run: |
          "$VENV_PYTHON" -m pip install pytest

      - name: Run pytest
        working-directory: Tools/context_compression
        run: |
          "$VENV_PYTHON" -m pytest -q -m "not integration"

  mcp-sandbox:
    name: mcp-sandbox
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Cache tool venv
        uses: actions/cache@v5
        with:
          path: Data/venvs/tools/mcp-sandbox
          key: aslm-chat-venv-mcp-sandbox-${{ runner.os }}-${{ hashFiles('Settings/venv_requirements.json') }}
          restore-keys: |
            aslm-chat-venv-mcp-sandbox-${{ runner.os }}-

      - name: Prepare tool venv
        run: |
          python -c "from Services import venv_manager; import sys; sys.exit(0 if venv_manager.ensure_venv('mcp-sandbox', log=True) else 1)"
          VENV_PYTHON=$(python -c "from Services import venv_manager; print(venv_manager.get_venv_python('mcp-sandbox'))")
          echo "VENV_PYTHON=${VENV_PYTHON}" >> "$GITHUB_ENV"
          test -x "$VENV_PYTHON"

      - name: Install pytest
        run: |
          "$VENV_PYTHON" -m pip install pytest

      - name: Run pytest
        working-directory: Tools/mcp-sandbox
        run: |
          "$VENV_PYTHON" -m pytest -q

  mcp-web-search:
    name: mcp-web-search
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Cache tool venv
        uses: actions/cache@v5
        with:
          path: Data/venvs/tools/mcp-web-search
          key: aslm-chat-venv-mcp-web-search-${{ runner.os }}-${{ hashFiles('Settings/venv_requirements.json') }}
          restore-keys: |
            aslm-chat-venv-mcp-web-search-${{ runner.os }}-

      - name: Prepare tool venv
        run: |
          python -c "from Services import venv_manager; import sys; sys.exit(0 if venv_manager.ensure_venv('mcp-web-search', log=True) else 1)"
          VENV_PYTHON=$(python -c "from Services import venv_manager; print(venv_manager.get_venv_python('mcp-web-search'))")
          echo "VENV_PYTHON=${VENV_PYTHON}" >> "$GITHUB_ENV"
          test -x "$VENV_PYTHON"

      - name: Install pytest
        run: |
          "$VENV_PYTHON" -m pip install pytest

      - name: Run pytest
        working-directory: Tools/mcp-web-search
        run: |
          "$VENV_PYTHON" -m pytest -q -m "not gliner and not integration and not live"

  mcp-browser-agent:
    name: mcp-browser-agent
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Cache tool venv
        uses: actions/cache@v5
        with:
          path: Data/venvs/tools/mcp-browser-agent
          key: aslm-chat-venv-mcp-browser-agent-${{ runner.os }}-${{ hashFiles('Settings/venv_requirements.json') }}
          restore-keys: |
            aslm-chat-venv-mcp-browser-agent-${{ runner.os }}-

      - name: Prepare tool venv
        run: |
          python -c "from Services import venv_manager; import sys; sys.exit(0 if venv_manager.ensure_venv('mcp-browser-agent', log=True) else 1)"
          VENV_PYTHON=$(python -c "from Services import venv_manager; print(venv_manager.get_venv_python('mcp-browser-agent'))")
          echo "VENV_PYTHON=${VENV_PYTHON}" >> "$GITHUB_ENV"
          test -x "$VENV_PYTHON"

      - name: Install pytest
        run: |
          "$VENV_PYTHON" -m pip install pytest

      - name: Run pytest
        working-directory: Tools/mcp-browser-agent
        run: |
          "$VENV_PYTHON" -m pytest -q
