---
title: "parameters-ui"
draft: false
---

## File `parameters-ui`

`Apps/UI/static/js/ui/parameters-ui.js` — ASLM Chat client script.

---

## Overview

Part of `Apps\UI\static\js\ui`. See **Related** for package index and callers.

---

## Public functions

#### `function createParametersUi(context)`

**Purpose:** Client function `createParametersUi` used by the chat shell.

#### `function normalizeToolServerId(serverId)`

**Purpose:** Client function `normalizeToolServerId` used by the chat shell.

#### `function availableToolServerIds()`

**Purpose:** Client function `availableToolServerIds` used by the chat shell.

#### `function updateAvailableToolServers(tools)`

**Purpose:** Client function `updateAvailableToolServers` used by the chat shell.

#### `function applySelectedToolServerIds(ids)`

**Purpose:** Client function `applySelectedToolServerIds` used by the chat shell.

#### `function toolServerIconClass(server)`

**Purpose:** Client function `toolServerIconClass` used by the chat shell.

#### `function renderToolServerList($target, hasToolSupport)`

**Purpose:** Client function `renderToolServerList` used by the chat shell.

#### `function renderToolControls()`

**Purpose:** Client function `renderToolControls` used by the chat shell.

#### `function renderMcpControls()`

**Purpose:** Client function `renderMcpControls` used by the chat shell.

#### `function escapeHtmlPlain(value)`

**Purpose:** Client function `escapeHtmlPlain` used by the chat shell.

#### `function highlightJsonHtml(source)`

**Purpose:** Client function `highlightJsonHtml` used by the chat shell.

#### `function buildGutterLines(lineCount)`

**Purpose:** Client function `buildGutterLines` used by the chat shell.

#### `function refreshToolServersAfterMcpSave()`

**Purpose:** Client function `refreshToolServersAfterMcpSave` used by the chat shell.

#### `function openMcpJsonEditor()`

**Purpose:** Client function `openMcpJsonEditor` used by the chat shell.

#### `function syncLayers()`

**Purpose:** Client function `syncLayers` used by the chat shell.

#### `function syncScroll()`

**Purpose:** Client function `syncScroll` used by the chat shell.

#### `function closeModal()`

**Purpose:** Client function `closeModal` used by the chat shell.

#### `function showModelPlaceholder(message)`

**Purpose:** Client function `showModelPlaceholder` used by the chat shell.

#### `function resetDynamicPanels()`

**Purpose:** Client function `resetDynamicPanels` used by the chat shell.

#### `function getSupportedParameterDefinitions(engine)`

**Purpose:** Client function `getSupportedParameterDefinitions` used by the chat shell.

#### `function getParameterGroup(paramKey)`

**Purpose:** Client function `getParameterGroup` used by the chat shell.

#### `function inferExperimentalParameterType(key, value)`

**Purpose:** Client function `inferExperimentalParameterType` used by the chat shell.

#### `function formatExperimentalParameterLabel(key)`

**Purpose:** Client function `formatExperimentalParameterLabel` used by the chat shell.

#### `function localizedParamLabel(key, config)`

**Purpose:** Client function `localizedParamLabel` used by the chat shell.

#### `function localizedParamNote(key, config)`

**Purpose:** Client function `localizedParamNote` used by the chat shell.

#### `function getParameterNote(config, paramKey)`

**Purpose:** Client function `getParameterNote` used by the chat shell.

#### `function formatParameterMetaValue(value)`

**Purpose:** Client function `formatParameterMetaValue` used by the chat shell.

#### `function getParameterMeta(config)`

**Purpose:** Client function `getParameterMeta` used by the chat shell.

#### `function getInputPlaceholder(config)`

**Purpose:** Client function `getInputPlaceholder` used by the chat shell.

#### `function renderParameterMeta(config)`

**Purpose:** Client function `renderParameterMeta` used by the chat shell.

#### `function buildTokenStepValues(minValue, maxValue)`

**Purpose:** Client function `buildTokenStepValues` used by the chat shell.

#### `function resolveTokenRangeValue(rawValue, allowedValues)`

**Purpose:** Client function `resolveTokenRangeValue` used by the chat shell.

#### `function renderKnownParameter(key, config, value, renderOptions)`

**Purpose:** Client function `renderKnownParameter` used by the chat shell.

#### `function renderExperimentalParameter(key, value)`

**Purpose:** Client function `renderExperimentalParameter` used by the chat shell.

#### `function updateVisibleDividers()`

**Purpose:** Client function `updateVisibleDividers` used by the chat shell.

#### `function renderThinkLevelControls()`

**Purpose:** Client function `renderThinkLevelControls` used by the chat shell.

#### `function labelForThinkOption(normalizedValue)`

**Purpose:** Client function `labelForThinkOption` used by the chat shell.

#### `function getThinkLevelLabel(value)`

**Purpose:** Client function `getThinkLevelLabel` used by the chat shell.

#### `function updateThinkControls()`

**Purpose:** Client function `updateThinkControls` used by the chat shell.

#### `function renderModelParameters(modelInfo, defaults)`

**Purpose:** Client function `renderModelParameters` used by the chat shell.

#### `function collectParameterPayload(selector)`

**Purpose:** Client function `collectParameterPayload` used by the chat shell.

#### `function collectOptionsPayload()`

**Purpose:** Client function `collectOptionsPayload` used by the chat shell.

#### `function handleRangeInput($input)`

**Purpose:** Client function `handleRangeInput` used by the chat shell.

#### `function handleNumberInput($input)`

**Purpose:** Client function `handleNumberInput` used by the chat shell.

#### `function normalizeOptionalNumericInput($input)`

**Purpose:** Client function `normalizeOptionalNumericInput` used by the chat shell.

#### `function toggleOptionalParameter($toggle)`

**Purpose:** Client function `toggleOptionalParameter` used by the chat shell.

#### `function getSelectedToolServerIds()`

**Purpose:** Client function `getSelectedToolServerIds` used by the chat shell.

---

## Related

- [ui/_index](../../../../../_index/)
