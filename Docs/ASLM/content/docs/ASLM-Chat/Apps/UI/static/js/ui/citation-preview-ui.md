---
title: "citation-preview-ui"
draft: false
---

## File `citation-preview-ui`

`Apps/UI/static/js/ui/citation-preview-ui.js` — ASLM Chat client script.

---

## Overview

Part of `Apps\UI\static\js\ui`. See **Related** for package index and callers.

---

## Public functions

#### `function parseCitationPreviewData(chip)`

**Purpose:** Client function `parseCitationPreviewData` used by the chat shell.

#### `function previewEvidenceLabel(value)`

**Purpose:** Client function `previewEvidenceLabel` used by the chat shell.

#### `function renderCitationPreviewHtml(data)`

**Purpose:** Client function `renderCitationPreviewHtml` used by the chat shell.

#### `function fallbackCopyText(text, onDone)`

**Purpose:** Client function `fallbackCopyText` used by the chat shell.

#### `function copyTextToClipboard(text, onDone)`

**Purpose:** Client function `copyTextToClipboard` used by the chat shell.

#### `function bindCitationPreviewCards(root)`

**Purpose:** Client function `bindCitationPreviewCards` used by the chat shell.

#### `function cancelOpen()`

**Purpose:** Client function `cancelOpen` used by the chat shell.

#### `function cancelClose()`

**Purpose:** Client function `cancelClose` used by the chat shell.

#### `function hidePreview()`

**Purpose:** Client function `hidePreview` used by the chat shell.

#### `function scheduleClose()`

**Purpose:** Client function `scheduleClose` used by the chat shell.

#### `function ensurePreview()`

**Purpose:** Client function `ensurePreview` used by the chat shell.

#### `function positionPreview(chip)`

**Purpose:** Client function `positionPreview` used by the chat shell.

#### `function showPreview(chip)`

**Purpose:** Client function `showPreview` used by the chat shell.

#### `function scheduleOpen(chip)`

**Purpose:** Client function `scheduleOpen` used by the chat shell.

---

## Related

- [ui/_index](../../../../../_index/)
