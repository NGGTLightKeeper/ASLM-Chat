---
title: "utils"
draft: false
---

## File `utils`

`Apps/UI/static/js/main/utils.js` — ASLM Chat client script.

---

## Overview

Part of `Apps\UI\static\js\main`. See **Related** for package index and callers.

---

## Public functions

#### `function parseJsonScript(id)`

**Purpose:** Client function `parseJsonScript` used by the chat shell.

#### `function normalizeParameterName(param)`

**Purpose:** Client function `normalizeParameterName` used by the chat shell.

#### `function isThinkingParameterKey(param)`

**Purpose:** Client function `isThinkingParameterKey` used by the chat shell.

#### `function normalizeAddressForParsing(value)`

**Purpose:** Client function `normalizeAddressForParsing` used by the chat shell.

#### `function isLocalHostname(hostname)`

**Purpose:** Client function `isLocalHostname` used by the chat shell.

#### `function timeNow(dateInput)`

**Purpose:** Client function `timeNow` used by the chat shell.

#### `function escHtml(str)`

**Purpose:** Client function `escHtml` used by the chat shell.

#### `function escapeAttributeValue(str)`

**Purpose:** Client function `escapeAttributeValue` used by the chat shell.

#### `function escapeTextareaValue(str)`

**Purpose:** Client function `escapeTextareaValue` used by the chat shell.

#### `function getNestedValue(source, path)`

**Purpose:** Client function `getNestedValue` used by the chat shell.

#### `function setNestedValue(target, path, value)`

**Purpose:** Client function `setNestedValue` used by the chat shell.

#### `function deleteNestedValue(target, path)`

**Purpose:** Client function `deleteNestedValue` used by the chat shell.

#### `function flattenConfigLeaves(source, prefix)`

**Purpose:** Client function `flattenConfigLeaves` used by the chat shell.

---

## Related

- [main/_index](../../../../../_index/)
