---
title: "i18n"
draft: false
---

## File `i18n`

`Apps/UI/static/js/main/i18n.js` — ASLM Chat client script.

---

## Public functions

#### `function lookupNested(catalog, key)`

**Purpose:** Client function `lookupNested` used by the chat shell.

#### `function interpolate(template, params)`

**Purpose:** Client function `interpolate` used by the chat shell.

#### `function initI18n()`

**Purpose:** Client function `initI18n` used by the chat shell.

#### `function getEffectiveLocale()`

**Purpose:** Client function `getEffectiveLocale` used by the chat shell.

#### `function isRtl()`

**Purpose:** Client function `isRtl` used by the chat shell.

#### `function t(key, params, fallback)`

**Purpose:** Client function `t` used by the chat shell.

#### `function intlLocaleTag()`

**Purpose:** Client function `intlLocaleTag` used by the chat shell.

---

## Related

- [main/_index](../../../../../_index/)
