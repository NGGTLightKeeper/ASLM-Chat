---
title: "citations-ui"
draft: false
---

## File `citations-ui`

`Apps/UI/static/js/ui/citations-ui.js` — ASLM Chat client script.

---

## Overview

Part of `Apps\UI\static\js\ui`. See **Related** for package index and callers.

---

## Public functions

#### `function isCitationDashCodePoint(codePoint)`

**Purpose:** Client function `isCitationDashCodePoint` used by the chat shell.

#### `function normalizeCitationHandleGlyphs(value)`

**Purpose:** Client function `normalizeCitationHandleGlyphs` used by the chat shell.

#### `function normalizeCitationId(value)`

**Purpose:** Client function `normalizeCitationId` used by the chat shell.

#### `function isCitationHandleId(value)`

**Purpose:** Client function `isCitationHandleId` used by the chat shell.

#### `function normalizeCitationBrackets(value)`

**Purpose:** Client function `normalizeCitationBrackets` used by the chat shell.

#### `function normalizeCitationSpacing(value)`

**Purpose:** Client function `normalizeCitationSpacing` used by the chat shell.

#### `function createCitationRegistry()`

**Purpose:** Client function `createCitationRegistry` used by the chat shell.

#### `function safeExternalUrl(value)`

**Purpose:** Client function `safeExternalUrl` used by the chat shell.

#### `function safeFaviconUrl(value)`

**Purpose:** Client function `safeFaviconUrl` used by the chat shell.

#### `function domainFromUrl(value)`

**Purpose:** Client function `domainFromUrl` used by the chat shell.

#### `function faviconUrlForDomain(domain)`

**Purpose:** Client function `faviconUrlForDomain` used by the chat shell.

#### `function readSourceDomain(source)`

**Purpose:** Client function `readSourceDomain` used by the chat shell.

#### `function fieldFromCitationBlock(block, fieldName)`

**Purpose:** Client function `fieldFromCitationBlock` used by the chat shell.

#### `function compactText(value, maxLength)`

**Purpose:** Client function `compactText` used by the chat shell.

#### `function displayTitleForSource(source, domain)`

**Purpose:** Client function `displayTitleForSource` used by the chat shell.

#### `function normalizeCitationSource(source, rank)`

**Purpose:** Client function `normalizeCitationSource` used by the chat shell.

#### `function sourceIds(source)`

**Purpose:** Client function `sourceIds` used by the chat shell.

#### `function collectSourceCandidates(container)`

**Purpose:** Client function `collectSourceCandidates` used by the chat shell.

#### `function parseToolResultObject(segment)`

**Purpose:** Client function `parseToolResultObject` used by the chat shell.

#### `function parseTextCitationSources(text)`

**Purpose:** Client function `parseTextCitationSources` used by the chat shell.

#### `function citationSourceForId(citationRegistry, sourceId)`

**Purpose:** Client function `citationSourceForId` used by the chat shell.

#### `function addCitationSource(citationRegistry, source, rank)`

**Purpose:** Client function `addCitationSource` used by the chat shell.

#### `function addSegmentCitationSources(citationRegistry, segment)`

**Purpose:** Client function `addSegmentCitationSources` used by the chat shell.

#### `function addSegmentsCitationSources(citationRegistry, segments)`

**Purpose:** Client function `addSegmentsCitationSources` used by the chat shell.

#### `function extractCitationIds(value, citationRegistry)`

**Purpose:** Client function `extractCitationIds` used by the chat shell.

#### `function addId(candidate)`

**Purpose:** Client function `addId` used by the chat shell.

#### `function hasCitationHandle(value)`

**Purpose:** Client function `hasCitationHandle` used by the chat shell.

#### `function renderCitationChip(source, sourceId)`

**Purpose:** Client function `renderCitationChip` used by the chat shell.

#### `function decorateCitationsInHtml(html, citationRegistry)`

**Purpose:** Client function `decorateCitationsInHtml` used by the chat shell.

#### `function walk(node)`

**Purpose:** Client function `walk` used by the chat shell.

---

## Related

- [ui/_index](../../../../../_index/)
