---
title: "api"
draft: false
---

## File `api`

`Apps/UI/static/js/main/api.js` — ASLM Chat client script.

---

## Public functions

#### `function getCookie(name)`

**Purpose:** Client function `getCookie` used by the chat shell.

#### `function getCsrfToken()`

**Purpose:** Client function `getCsrfToken` used by the chat shell.

#### `function requestJson(url, options)`

**Purpose:** Client function `requestJson` used by the chat shell.

#### `function getJson(url, options)`

**Purpose:** Client function `getJson` used by the chat shell.

#### `function postJson(url, payload, options)`

**Purpose:** Client function `postJson` used by the chat shell.

#### `function patchJson(url, payload, options)`

**Purpose:** Client function `patchJson` used by the chat shell.

#### `function deleteJson(url, options)`

**Purpose:** Client function `deleteJson` used by the chat shell.

---

## Related

- [main/_index](../../../../../_index/)
