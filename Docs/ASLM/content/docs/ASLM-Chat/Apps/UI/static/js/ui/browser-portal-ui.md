---
title: "browser-portal-ui"
draft: false
---

## File `browser-portal-ui`

`Apps/UI/static/js/ui/browser-portal-ui.js` — ASLM Chat client script.

---

## Overview

Part of `Apps\UI\static\js\ui`. See **Related** for package index and callers.

---

## Public functions

#### `function createBrowserPortalUi(context)`

**Purpose:** Client function `createBrowserPortalUi` used by the chat shell.

#### `function normalizeToolId(segment)`

**Purpose:** Client function `normalizeToolId` used by the chat shell.

#### `function isBrowserToolSegment(segment)`

**Purpose:** Client function `isBrowserToolSegment` used by the chat shell.

#### `function browserDebugEnabled(options)`

**Purpose:** Client function `browserDebugEnabled` used by the chat shell.

#### `function statusForSegments(segments, options)`

**Purpose:** Client function `statusForSegments` used by the chat shell.

#### `function statusLabel(status)`

**Purpose:** Client function `statusLabel` used by the chat shell.

#### `function actionForSegment(segment)`

**Purpose:** Client function `actionForSegment` used by the chat shell.

#### `function dataUrlFromImage(image)`

**Purpose:** Client function `dataUrlFromImage` used by the chat shell.

#### `function extractFrame(segments)`

**Purpose:** Client function `extractFrame` used by the chat shell.

#### `function extractUrl(segments, frame)`

**Purpose:** Client function `extractUrl` used by the chat shell.

#### `function renderFrame(frame, status)`

**Purpose:** Client function `renderFrame` used by the chat shell.

#### `function renderPortalSegment(browserSegments, options)`

**Purpose:** Client function `renderPortalSegment` used by the chat shell.

#### `function hydrate(root)`

**Purpose:** Client function `hydrate` used by the chat shell.

#### `function numericPayloadValue(value)`

**Purpose:** Client function `numericPayloadValue` used by the chat shell.

#### `function epochSecondsToMs(value)`

**Purpose:** Client function `epochSecondsToMs` used by the chat shell.

#### `function waitDeadlineAtMs()`

**Purpose:** Client function `waitDeadlineAtMs` used by the chat shell.

#### `function armFramePollInterval()`

**Purpose:** Client function `armFramePollInterval` used by the chat shell.

#### `function onFramePollVisibility()`

**Purpose:** Client function `onFramePollVisibility` used by the chat shell.

#### `function detachFramePollVisibility()`

**Purpose:** Client function `detachFramePollVisibility` used by the chat shell.

#### `function portalLiveUiShowsSessionEnded()`

**Purpose:** Client function `portalLiveUiShowsSessionEnded` used by the chat shell.

#### `function frameFromPayload(payload)`

**Purpose:** Client function `frameFromPayload` used by the chat shell.

#### `function ensureKeyCapture()`

**Purpose:** Client function `ensureKeyCapture` used by the chat shell.

#### `function focusKeyCapture()`

**Purpose:** Client function `focusKeyCapture` used by the chat shell.

#### `function applyFramePayload(payload)`

**Purpose:** Client function `applyFramePayload` used by the chat shell.

#### `function setManualStatus(text)`

**Purpose:** Client function `setManualStatus` used by the chat shell.

#### `function setPortalStatus(statusName, text)`

**Purpose:** Client function `setPortalStatus` used by the chat shell.

#### `function cancelWaitTimer()`

**Purpose:** Client function `cancelWaitTimer` used by the chat shell.

#### `function stopBrowserPortalManualLoop(reason)`

**Purpose:** Client function `stopBrowserPortalManualLoop` used by the chat shell.

#### `function addClickRing(event)`

**Purpose:** Client function `addClickRing` used by the chat shell.

#### `function browserImageGeometry()`

**Purpose:** Client function `browserImageGeometry` used by the chat shell.

#### `function pollFrameOnce()`

**Purpose:** Client function `pollFrameOnce` used by the chat shell.

#### `function sendPortalEvent(type, payload)`

**Purpose:** Client function `sendPortalEvent` used by the chat shell.

#### `function sendTypedText(text)`

**Purpose:** Client function `sendTypedText` used by the chat shell.

#### `function handlePortalKeyDown(event)`

**Purpose:** Client function `handlePortalKeyDown` used by the chat shell.

#### `function handlePortalPaste(event)`

**Purpose:** Client function `handlePortalPaste` used by the chat shell.

#### `function handleCaptureInput(event)`

**Purpose:** Client function `handleCaptureInput` used by the chat shell.

#### `function tick()`

**Purpose:** Client function `tick` used by the chat shell.

#### `function enhanceSegments(segments, options)`

**Purpose:** Client function `enhanceSegments` used by the chat shell.

---

## Related

- [ui/_index](../../../../../_index/)
