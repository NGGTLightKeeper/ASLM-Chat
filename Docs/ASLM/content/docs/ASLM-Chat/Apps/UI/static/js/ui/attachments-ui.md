---
title: "attachments-ui"
draft: false
---

## File `attachments-ui`

`Apps/UI/static/js/ui/attachments-ui.js` — ASLM Chat client script.

---

## Overview

Part of `Apps\UI\static\js\ui`. See **Related** for package index and callers.

---

## Public functions

#### `function createAttachmentsUi(context)`

**Purpose:** Client function `createAttachmentsUi` used by the chat shell.

#### `function setUpdateSendButtons(fn)`

**Purpose:** Client function `setUpdateSendButtons` used by the chat shell.

#### `function isInlineDataUrl(value)`

**Purpose:** Client function `isInlineDataUrl` used by the chat shell.

#### `function dataUrlToBase64(value)`

**Purpose:** Client function `dataUrlToBase64` used by the chat shell.

#### `function readBlobAsDataUrl(blob)`

**Purpose:** Client function `readBlobAsDataUrl` used by the chat shell.

#### `function updateAttachmentControls()`

**Purpose:** Client function `updateAttachmentControls` used by the chat shell.

#### `function clearPendingAttachments()`

**Purpose:** Client function `clearPendingAttachments` used by the chat shell.

#### `function normalizeAttachment(attachment)`

**Purpose:** Client function `normalizeAttachment` used by the chat shell.

#### `function resolveAttachmentData(attachment)`

**Purpose:** Client function `resolveAttachmentData` used by the chat shell.

#### `function previewLabel(attachment)`

**Purpose:** Client function `previewLabel` used by the chat shell.

#### `function uploadIconLabel(attachment)`

**Purpose:** Client function `uploadIconLabel` used by the chat shell.

#### `function isImageFile(file)`

**Purpose:** Client function `isImageFile` used by the chat shell.

#### `function displayKindForFile(file)`

**Purpose:** Client function `displayKindForFile` used by the chat shell.

#### `function rebuildPreviewStrips()`

**Purpose:** Client function `rebuildPreviewStrips` used by the chat shell.

#### `function removePendingAttachment(index)`

**Purpose:** Client function `removePendingAttachment` used by the chat shell.

#### `function readFileAsDataUrl(file)`

**Purpose:** Client function `readFileAsDataUrl` used by the chat shell.

#### `function uploadOneFile(file, pendingAttachment)`

**Purpose:** Client function `uploadOneFile` used by the chat shell.

#### `function queueFiles(files)`

**Purpose:** Client function `queueFiles` used by the chat shell.

#### `function clipboardImageName(mimeType, index)`

**Purpose:** Client function `clipboardImageName` used by the chat shell.

#### `function normalizeClipboardImageFile(file, index)`

**Purpose:** Client function `normalizeClipboardImageFile` used by the chat shell.

#### `function collectClipboardImageFiles(clipboardData)`

**Purpose:** Client function `collectClipboardImageFiles` used by the chat shell.

#### `function addFile(file)`

**Purpose:** Client function `addFile` used by the chat shell.

#### `function handleClipboardPaste(clipboardData)`

**Purpose:** Client function `handleClipboardPaste` used by the chat shell.

#### `function handleFileInput(event)`

**Purpose:** Client function `handleFileInput` used by the chat shell.

#### `function handleDroppedFiles(files)`

**Purpose:** Client function `handleDroppedFiles` used by the chat shell.

---

## Related

- [ui/_index](../../../../../_index/)
