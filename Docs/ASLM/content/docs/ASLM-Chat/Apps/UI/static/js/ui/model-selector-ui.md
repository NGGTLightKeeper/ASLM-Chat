---
title: "model-selector-ui"
draft: false
---

## File `model-selector-ui`

`Apps/UI/static/js/ui/model-selector-ui.js` — ASLM Chat client script.

---

## Overview

Part of `Apps\UI\static\js\ui`. See **Related** for package index and callers.

---

## Public functions

#### `function createModelSelectorUi(context)`

**Purpose:** Client function `createModelSelectorUi` used by the chat shell.

#### `function selectedModel()`

**Purpose:** Client function `selectedModel` used by the chat shell.

#### `function cacheKey(modelName)`

**Purpose:** Client function `cacheKey` used by the chat shell.

#### `function modelsFromNativeSelect()`

**Purpose:** Client function `modelsFromNativeSelect` used by the chat shell.

#### `function isPlaceholderModel(model)`

**Purpose:** Client function `isPlaceholderModel` used by the chat shell.

#### `function currentCapabilities(modelName)`

**Purpose:** Client function `currentCapabilities` used by the chat shell.

#### `function cacheCapabilities(key, value)`

**Purpose:** Client function `cacheCapabilities` used by the chat shell.

#### `function rememberCurrentModelCapabilities()`

**Purpose:** Client function `rememberCurrentModelCapabilities` used by the chat shell.

#### `function updateButtonLabel()`

**Purpose:** Client function `updateButtonLabel` used by the chat shell.

#### `function applyFilter()`

**Purpose:** Client function `applyFilter` used by the chat shell.

#### `function requestVisibleCapabilities()`

**Purpose:** Client function `requestVisibleCapabilities` used by the chat shell.

#### `function processCapabilityQueue()`

**Purpose:** Client function `processCapabilityQueue` used by the chat shell.

#### `function optionHtml(model, index)`

**Purpose:** Client function `optionHtml` used by the chat shell.

#### `function renderList()`

**Purpose:** Client function `renderList` used by the chat shell.

#### `function syncFromNative()`

**Purpose:** Client function `syncFromNative` used by the chat shell.

#### `function chooseModelByIndex(index)`

**Purpose:** Client function `chooseModelByIndex` used by the chat shell.

#### `function scrollHighlightedIntoView()`

**Purpose:** Client function `scrollHighlightedIntoView` used by the chat shell.

#### `function positionPopover()`

**Purpose:** Client function `positionPopover` used by the chat shell.

#### `function open()`

**Purpose:** Client function `open` used by the chat shell.

#### `function close()`

**Purpose:** Client function `close` used by the chat shell.

#### `function toggle()`

**Purpose:** Client function `toggle` used by the chat shell.

#### `function bindEvents()`

**Purpose:** Client function `bindEvents` used by the chat shell.

#### `function init()`

**Purpose:** Client function `init` used by the chat shell.

---

## Related

- [ui/_index](../../../../../_index/)
