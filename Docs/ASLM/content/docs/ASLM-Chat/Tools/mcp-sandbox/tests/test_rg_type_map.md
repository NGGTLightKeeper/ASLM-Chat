---
title: "test_rg_type_map"
draft: false
---

## Module `test_rg_type_map`

`Tools/mcp-sandbox/tests/test_rg_type_map.py` — ASLM Chat Python module.

---

## Test methods

#### `def test_returns_nonempty_dict()`

**Purpose:** _load_rg_type_map.

#### `def test_common_types_present()`

**Purpose:** Implements `test_common_types_present` in `test_rg_type_map.py`.

**Steps:**

1. Iterate and transform or accumulate state.

#### `def test_glob_format()`

**Purpose:** Implements `test_glob_format` in `test_rg_type_map.py`.

**Steps:**

1. Iterate and transform or accumulate state.

#### `def test_short_alias_derived()`

**Purpose:** 'py' alias must be derived even though rg uses 'python' as canonical name.

#### `def test_fallback_on_rg_unavailable()`

**Purpose:** When rg is unavailable, must return fallback dict.

**Steps:**

1. Spawn or communicate with a child process.

#### `def test_fallback_on_rg_nonzero_exit()`

**Purpose:** Non-zero rg exit code triggers fallback.

**Steps:**

1. Spawn or communicate with a child process.

#### `def test_module_level_map_is_dict()`

**Purpose:** Module-level RG_TYPE_TO_GLOB.

#### `def test_code_extensions_nonempty()`

**Purpose:** _CODE_EXTENSIONS derivation.

#### `def test_code_extensions_no_overlap_with_text()`

**Purpose:** Implements `test_code_extensions_no_overlap_with_text` in `test_rg_type_map.py`.

#### `def test_code_extensions_contain_core_languages()`

**Purpose:** Implements `test_code_extensions_contain_core_languages` in `test_rg_type_map.py`.

#### `def test_code_extensions_all_start_with_dot()`

**Purpose:** Implements `test_code_extensions_all_start_with_dot` in `test_rg_type_map.py`.

---

## Related

- [tests/_index](../../../_index/)
