---
title: "test_native_exec"
draft: false
---

## Module `test_native_exec`

`Tools/mcp-sandbox/tests/test_native_exec.py` — ASLM Chat Python module.

---

## Classes

### `class TextProcess`

**Purpose:** Type `TextProcess` defined in `test_native_exec.py`.

### `class NativeExecTests`

**Purpose:** Type `NativeExecTests` defined in `test_native_exec.py`.

---

## Test methods

#### `def TextProcess.__init__(*, stdout=…, stderr=…, returncode=…) -> None`

**Purpose:** Implements `TextProcess.__init__` in `test_native_exec.py`.

#### `def TextProcess.poll() -> int | None`

**Purpose:** Implements `TextProcess.poll` in `test_native_exec.py`.

#### `def TextProcess.kill() -> None`

**Purpose:** Implements `TextProcess.kill` in `test_native_exec.py`.

#### `def NativeExecTests.setUp() -> None`

**Purpose:** Implements `NativeExecTests.setUp` in `test_native_exec.py`.

**Steps:**

1. Iterate and transform or accumulate state.

#### `def NativeExecTests.test_native_exec_uses_task_root_cwd_and_process_group() -> None`

**Purpose:** Implements `NativeExecTests.test_native_exec_uses_task_root_cwd_and_process_group` in `test_native_exec.py`.

**Steps:**

1. Spawn or communicate with a child process.

#### `def NativeExecTests.test_native_exec_sets_home_for_command_user() -> None`

**Purpose:** Implements `NativeExecTests.test_native_exec_sets_home_for_command_user` in `test_native_exec.py`.

**Steps:**

1. Spawn or communicate with a child process.

#### `def NativeExecTests.test_native_timeout_kills_process_group_without_container_restart() -> None`

**Purpose:** Implements `NativeExecTests.test_native_timeout_kills_process_group_without_container_restart` in `test_native_exec.py`.

**Steps:**

1. Return the computed result to the caller.
2. Spawn or communicate with a child process.

---

## Related

- [tests/_index](../../../_index/)
