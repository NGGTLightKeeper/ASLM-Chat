---
title: "test_native_background"
draft: false
---

## Module `test_native_background`

`Tools/mcp-sandbox/tests/feature_jobs_edit/test_native_background.py` — ASLM Chat Python module.

---

## Classes

### `class BackgroundProcess`

**Purpose:** Type `BackgroundProcess` defined in `test_native_background.py`.

### `class NativeBackgroundTests`

**Purpose:** Type `NativeBackgroundTests` defined in `test_native_background.py`.

---

## Test methods

#### `def BackgroundProcess.__init__() -> None`

**Purpose:** Implements `BackgroundProcess.__init__` in `test_native_background.py`.

#### `def BackgroundProcess.poll()`

**Purpose:** Implements `BackgroundProcess.poll` in `test_native_background.py`.

#### `def BackgroundProcess.kill() -> None`

**Purpose:** Implements `BackgroundProcess.kill` in `test_native_background.py`.

#### `def NativeBackgroundTests.setUp() -> None`

**Purpose:** Implements `NativeBackgroundTests.setUp` in `test_native_background.py`.

#### `def NativeBackgroundTests.test_native_timeout_backgrounds_without_killing_process_group() -> None`

**Purpose:** Implements `NativeBackgroundTests.test_native_timeout_backgrounds_without_killing_process_group` in `test_native_background.py`.

**Steps:**

1. Spawn or communicate with a child process.

#### `def NativeBackgroundTests.test_foreground_native_job_finalizes_exit_code() -> None`

**Purpose:** Implements `NativeBackgroundTests.test_foreground_native_job_finalizes_exit_code` in `test_native_background.py`.

**Steps:**

1. Spawn or communicate with a child process.

---

## Related

- [feature_jobs_edit/_index](../../../../_index/)
