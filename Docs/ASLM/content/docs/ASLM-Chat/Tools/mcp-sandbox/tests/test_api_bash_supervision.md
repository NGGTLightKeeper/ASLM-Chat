---
title: "test_api_bash_supervision"
draft: false
---

## Module `test_api_bash_supervision`

`Tools/mcp-sandbox/tests/test_api_bash_supervision.py` — ASLM Chat Python module.

---

## Test methods

#### `def test_plain_cat_large_file_uses_structured_preview() -> None`

**Purpose:** Implements `test_plain_cat_large_file_uses_structured_preview` in `test_api_bash_supervision.py`.

**Steps:**

1. Iterate and transform or accumulate state.

#### `def test_grep_routes_to_real_bash_not_legacy_controller() -> None`

**Purpose:** Implements `test_grep_routes_to_real_bash_not_legacy_controller` in `test_api_bash_supervision.py`.

#### `def test_compound_shell_command_never_uses_file_preview_shortcut() -> None`

**Purpose:** Implements `test_compound_shell_command_never_uses_file_preview_shortcut` in `test_api_bash_supervision.py`.

---

## Private functions

#### `def _clean_task_root() -> None`

**Purpose:** Implements `_clean_task_root` in `test_api_bash_supervision.py`.

**Steps:**

1. Iterate and transform or accumulate state.

---

## Related

- [tests/_index](../../../_index/)
