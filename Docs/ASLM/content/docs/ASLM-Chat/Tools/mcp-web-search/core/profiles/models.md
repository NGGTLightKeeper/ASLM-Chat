---
title: "models"
draft: false
---

## Module `models`

`Tools/mcp-web-search/core/profiles/models.py` — ASLM Chat Python module.

---

## Constants

### `METHOD_BROWSER`

Warm persistent stealth-browser (cloakbrowser daemon). The cost-aware method selector ranks it above the HTTP methods but below camoufox subprocess.

## Classes

### `class FetchOutcome`

**Purpose:** Type `FetchOutcome` defined in `models.py`.

### `class ProfileHint`

**Purpose:** Type `ProfileHint` defined in `models.py`.

### `class DomainProfile`

**Purpose:** Type `DomainProfile` defined in `models.py`.
