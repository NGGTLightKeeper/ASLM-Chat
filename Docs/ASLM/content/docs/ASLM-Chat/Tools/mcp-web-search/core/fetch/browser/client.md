---
title: "client"
draft: false
---

## Module `client`

`Tools/mcp-web-search/core/fetch/browser/client.py` — ASLM Chat Python module.

---

## Classes

### `class BrowserClient`

**Purpose:** Type `BrowserClient` defined in `client.py`.

---

## Public functions

#### `def get_browser_client() -> BrowserClient`

**Purpose:** Implements `get_browser_client`.

**Steps:**

1. Return the computed result to the caller.

#### `async def browser_available() -> bool`

**Purpose:** Implements `browser_available`.

**Steps:**

1. Return the computed result to the caller.

#### `async def browser_fetch(url) -> BrowserFetch`

**Purpose:** Implements `browser_fetch`.

**Steps:**

1. Return the computed result to the caller.

#### `async def shutdown_browser()`

**Purpose:** Implements `shutdown_browser`.

**Steps:**

1. Return the computed result to the caller.

---

## Related

- [browser/_index](../_index/)
