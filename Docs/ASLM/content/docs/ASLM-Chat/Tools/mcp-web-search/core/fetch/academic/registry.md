---
title: "registry"
draft: false
---

## Module `registry`

`Tools/mcp-web-search/core/fetch/academic/registry.py` — ASLM Chat Python module.

---

## Overview

Part of `Tools/mcp-web-search/core/fetch/academic`.

---

## Classes

### `class AcademicDomain`

**Purpose:** Implements `AcademicDomain`.

#### `def AcademicDomain.host(self) -> str`

**Purpose:** Implements `host`.

---

## Public functions

#### `def load_registry() -> tuple[...]`

**Purpose:** Implements `load_registry`.

#### `def json_api_domains() -> tuple[...]`

**Purpose:** Implements `json_api_domains`.

#### `def domain_for(host) -> ...`

**Purpose:** Implements `domain_for`.

---

## Private functions

#### `def _coerce(entry) -> AcademicDomain`

**Purpose:** Implements `_coerce`.

---

## Related

- [academic/_index](../../_index/)
