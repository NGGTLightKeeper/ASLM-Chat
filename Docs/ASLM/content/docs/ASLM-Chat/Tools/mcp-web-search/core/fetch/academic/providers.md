---
title: "providers"
draft: false
---

## Module `providers`

`Tools/mcp-web-search/core/fetch/academic/providers.py` — ASLM Chat Python module.

---

## Overview

Part of `Tools/mcp-web-search/core/fetch/academic`.

---

## Classes

### `class AcademicProvider`

**Purpose:** Implements `AcademicProvider`.

#### `def AcademicProvider.headers(self) -> ...`

**Purpose:** Implements `headers`.

#### `def AcademicProvider.domain(self) -> ...`

**Purpose:** Implements `domain`.

---

## Public functions

#### `def active_providers() -> tuple[...]`

**Purpose:** Implements `active_providers`.

#### `def ranked_providers() -> list[...]`

**Purpose:** Implements `ranked_providers`.

---

## Private functions

#### `def _q(query) -> str`

**Purpose:** Implements `_q`.

#### `def _path_q(query) -> str`

**Purpose:** Implements `_path_q`.

#### `def _has_serpapi_key() -> bool`

**Purpose:** Implements `_has_serpapi_key`.

---

## Related

- [academic/_index](../../_index/)
