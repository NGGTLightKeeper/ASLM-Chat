---
title: "mcp_contract"
draft: false
---

## Module `mcp_contract`

`Tools/mcp-web-search/core/mcp_contract.py` — ASLM Chat Python module.

---

## Overview

Part of `Tools/mcp-web-search/core`.

---

## Public functions

#### `def sanitize_query(query) -> str`

**Purpose:** Implements `sanitize_query`.

#### `def coerce_search_query(value) -> str`

**Purpose:** Implements `coerce_search_query`.

#### `def coerce_search_effort(value) -> str`

**Purpose:** Implements `coerce_search_effort`.

#### `def coerce_search_shopping(value) -> bool`

**Purpose:** Implements `coerce_search_shopping`.

#### `def coerce_search_academic(value) -> bool`

**Purpose:** Implements `coerce_search_academic`.

---

## Private functions

#### `def _try_parse_json(value) -> Any`

**Purpose:** Implements `_try_parse_json`.

#### `def _coerce_bool_opt(value, key) -> bool`

**Purpose:** Implements `_coerce_bool_opt`.

---

## Related

- [core/_index](../../_index/)
