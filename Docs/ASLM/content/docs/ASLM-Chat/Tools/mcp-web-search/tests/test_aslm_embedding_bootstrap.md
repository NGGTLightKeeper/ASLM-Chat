---
title: "test_aslm_embedding_bootstrap"
draft: false
---

## Module `test_aslm_embedding_bootstrap`

`Tools/mcp-web-search/tests/test_aslm_embedding_bootstrap.py` — ASLM Chat Python module.

---

## Test methods

#### `def test_maybe_migrate_legacy_dirs_renames_when_target_missing(tmp_path, monkeypatch) -> None`

**Purpose:** Implements `test_maybe_migrate_legacy_dirs_renames_when_target_missing` in `test_aslm_embedding_bootstrap.py`.

#### `def test_maybe_migrate_legacy_dirs_skips_when_target_exists(tmp_path, monkeypatch) -> None`

**Purpose:** Implements `test_maybe_migrate_legacy_dirs_skips_when_target_exists` in `test_aslm_embedding_bootstrap.py`.

#### `def test_ensure_embedding_export_skips_when_complete(tmp_path, monkeypatch) -> None`

**Purpose:** Implements `test_ensure_embedding_export_skips_when_complete` in `test_aslm_embedding_bootstrap.py`.

#### `def test_ensure_embedding_export_downloads_when_incomplete(tmp_path, monkeypatch) -> None`

**Purpose:** Implements `test_ensure_embedding_export_downloads_when_incomplete` in `test_aslm_embedding_bootstrap.py`.

#### `def test_ensure_embedding_export_raises_when_still_incomplete(tmp_path, monkeypatch) -> None`

**Purpose:** Implements `test_ensure_embedding_export_raises_when_still_incomplete` in `test_aslm_embedding_bootstrap.py`.

#### `def test_ensure_aslm_embedding_models_calls_both_exports(monkeypatch) -> None`

**Purpose:** Implements `test_ensure_aslm_embedding_models_calls_both_exports` in `test_aslm_embedding_bootstrap.py`.

---

## Related

- [tests/_index](../../../_index/)
