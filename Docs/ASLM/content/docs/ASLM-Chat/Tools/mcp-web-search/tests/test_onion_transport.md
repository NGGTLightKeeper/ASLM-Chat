---
title: "test_onion_transport"
draft: false
---

## Module `test_onion_transport`

`Tools/mcp-web-search/tests/test_onion_transport.py` — ASLM Chat Python module.

---

## Public functions

#### `def test_disabled_is_noop(monkeypatch)`

**Purpose:** Implements `test_disabled_is_noop`.

#### `def test_reuses_running_tor_before_spawning(monkeypatch)`

**Purpose:** Implements `test_reuses_running_tor_before_spawning`.

#### `def test_spawns_from_discovered_binary_when_none_running(monkeypatch)`

**Purpose:** Implements `test_spawns_from_discovered_binary_when_none_running`.

#### `def test_no_binary_keeps_feature_disabled(monkeypatch)`

**Purpose:** Implements `test_no_binary_keeps_feature_disabled`.

#### `def test_discover_binary_override(tmp_path)`

**Purpose:** Implements `test_discover_binary_override`.

#### `def test_fetch_unavailable_when_no_socks(monkeypatch)`

**Purpose:** Implements `test_fetch_unavailable_when_no_socks`.

#### `def test_fetch_ok_over_fake_tor(monkeypatch)`

**Purpose:** Implements `test_fetch_ok_over_fake_tor`.

#### `def test_looks_like_tb_tor_matcher(tmp_path)`

**Purpose:** Implements `test_looks_like_tb_tor_matcher`.

#### `def test_indexer_noop_on_windows(monkeypatch)`

**Purpose:** Implements `test_indexer_noop_on_windows`.

#### `def test_indexer_parses_locate_on_linux(tmp_path, monkeypatch)`

**Purpose:** Implements `test_indexer_parses_locate_on_linux`.

#### `def test_reap_if_idle(monkeypatch)`

**Purpose:** Implements `test_reap_if_idle`.

#### `def test_resolved_default_is_unset_not_none()`

**Purpose:** Implements `test_resolved_default_is_unset_not_none`.

#### `def test_prewarm_gating_and_background(monkeypatch)`

**Purpose:** Implements `test_prewarm_gating_and_background`.

---

## Private functions

#### `def _use_cfg(monkeypatch, **tor_kw)`

**Purpose:** Implements `_use_cfg`.

---

## Related

- [tests/_index](../../_index/)
