---
title: "test_search_cache"
draft: false
---

## Module `test_search_cache`

`Tools/mcp-web-search/tests/test_search_cache.py` — ASLM Chat Python module.

---

## Public functions

#### `def test_hosted_cache_roundtrip(tmp_path)`

**Purpose:** Implements `test_hosted_cache_roundtrip` in `test_search_cache.py`.

#### `def test_hosted_cache_key_is_param_sensitive(tmp_path)`

**Purpose:** Implements `test_hosted_cache_key_is_param_sensitive` in `test_search_cache.py`.

#### `def test_hosted_cache_normalizes_query(tmp_path)`

**Purpose:** Implements `test_hosted_cache_normalizes_query` in `test_search_cache.py`.

#### `def test_hosted_cache_expiry(tmp_path)`

**Purpose:** Implements `test_hosted_cache_expiry` in `test_search_cache.py`.

#### `def test_hosted_cache_negative_ttl_for_empty(tmp_path)`

**Purpose:** Implements `test_hosted_cache_negative_ttl_for_empty` in `test_search_cache.py`.

#### `def test_hosted_cache_evict_expired(tmp_path)`

**Purpose:** Implements `test_hosted_cache_evict_expired` in `test_search_cache.py`.

#### `def test_repeat_block_within_window()`

**Purpose:** Implements `test_repeat_block_within_window` in `test_search_cache.py`.

#### `def test_repeat_block_outside_window()`

**Purpose:** Implements `test_repeat_block_outside_window` in `test_search_cache.py`.

#### `def test_source_suppression()`

**Purpose:** Implements `test_source_suppression` in `test_search_cache.py`.

#### `def test_source_suppression_canonicalizes()`

**Purpose:** Implements `test_source_suppression_canonicalizes` in `test_search_cache.py`.

#### `def test_infer_pdf_url()`

**Purpose:** Implements `test_infer_pdf_url` in `test_search_cache.py`.

---

## Related

- [tests/_index](../_index/)
