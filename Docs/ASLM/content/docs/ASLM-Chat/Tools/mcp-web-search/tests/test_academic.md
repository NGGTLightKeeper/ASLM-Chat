---
title: "test_academic"
draft: false
---

## Module `test_academic`

`Tools/mcp-web-search/tests/test_academic.py` — ASLM Chat Python module.

---

## Overview

Part of `Tools/mcp-web-search/tests`.

---

## Public functions

#### `def test_registry_loads_friendly_json_apis()`

**Purpose:** Implements `test_registry_loads_friendly_json_apis`.

#### `def test_every_wired_provider_maps_to_a_registry_row()`

**Purpose:** Implements `test_every_wired_provider_maps_to_a_registry_row`.

#### `def test_parse_openalex_deinverts_abstract()`

**Purpose:** Implements `test_parse_openalex_deinverts_abstract`.

#### `def test_parse_crossref_strips_jats_abstract()`

**Purpose:** Implements `test_parse_crossref_strips_jats_abstract`.

#### `def test_parse_europepmc_and_doaj()`

**Purpose:** Implements `test_parse_europepmc_and_doaj`.

#### `def test_parse_arxiv_atom()`

**Purpose:** Implements `test_parse_arxiv_atom`.

#### `def test_parse_scholar_html()`

**Purpose:** Implements `test_parse_scholar_html`.

#### `def test_parse_serpapi_scholar()`

**Purpose:** Implements `test_parse_serpapi_scholar`.

#### `def test_scholar_path_is_mutually_exclusive(monkeypatch)`

**Purpose:** Implements `test_scholar_path_is_mutually_exclusive`.

#### `def test_scholar_block_detection()`

**Purpose:** Implements `test_scholar_block_detection`.

#### `def test_parsers_never_raise_on_garbage()`

**Purpose:** Implements `test_parsers_never_raise_on_garbage`.

#### `def test_dedupe_merges_by_doi_and_backfills()`

**Purpose:** Implements `test_dedupe_merges_by_doi_and_backfills`.

#### `def test_dual_key_dedup_collapses_doi_and_title()`

**Purpose:** Implements `test_dual_key_dedup_collapses_doi_and_title`.

#### `def test_source_saturation_prevents_monopoly()`

**Purpose:** Implements `test_source_saturation_prevents_monopoly`.

#### `def test_consensus_bonus_rewards_cross_index_agreement()`

**Purpose:** Implements `test_consensus_bonus_rewards_cross_index_agreement`.

#### `def test_title_dedupe_when_no_doi()`

**Purpose:** Implements `test_title_dedupe_when_no_doi`.

#### `def test_provider_health_cools_down_on_repeated_antibot()`

**Purpose:** Implements `test_provider_health_cools_down_on_repeated_antibot`.

#### `def test_provider_health_empty_200_is_not_a_failure()`

**Purpose:** Implements `test_provider_health_empty_200_is_not_a_failure`.

#### `def test_provider_health_min_interval_paces_fires()`

**Purpose:** Implements `test_provider_health_min_interval_paces_fires`.

#### `def test_provider_health_backoff_is_exponential()`

**Purpose:** Implements `test_provider_health_backoff_is_exponential`.

#### `def test_search_layer_adapter_shapes_citable_source()`

**Purpose:** Implements `test_search_layer_adapter_shapes_citable_source`.

---

## Related

- [tests/_index](../../_index/)
