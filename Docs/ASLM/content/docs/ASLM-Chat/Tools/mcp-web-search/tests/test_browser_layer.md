---
title: "test_browser_layer"
draft: false
---

## Module `test_browser_layer`

`Tools/mcp-web-search/tests/test_browser_layer.py` — ASLM Chat Python module.

---

## Classes

### `class _FakeResp`

**Purpose:** Type `_FakeResp` defined in `test_browser_layer.py`.

### `class _FakeHttp`

**Purpose:** Type `_FakeHttp` defined in `test_browser_layer.py`.

---

## Public functions

#### `def test_identity_checkpoint_and_latest_good(tmp_path)`

**Purpose:** Implements `test_identity_checkpoint_and_latest_good`.

**Steps:**

1. Return the computed result to the caller.

#### `def test_identity_prune_keeps_newest_good(tmp_path)`

**Purpose:** Implements `test_identity_prune_keeps_newest_good`.

**Steps:**

1. Return the computed result to the caller.

#### `def test_identity_rotate_burns_latest_and_falls_back(tmp_path)`

**Purpose:** Implements `test_identity_rotate_burns_latest_and_falls_back`.

**Steps:**

1. Return the computed result to the caller.

#### `def test_identity_rotate_with_no_fallback_returns_none(tmp_path)`

**Purpose:** Implements `test_identity_rotate_with_no_fallback_returns_none`.

**Steps:**

1. Return the computed result to the caller.

#### `def test_identity_cookie_header_is_host_scoped(tmp_path)`

**Purpose:** Implements `test_identity_cookie_header_is_host_scoped`.

**Steps:**

1. Return the computed result to the caller.

#### `def test_browser_section_defaults()`

**Purpose:** Implements `test_browser_section_defaults`.

**Steps:**

1. Return the computed result to the caller.

#### `def test_browser_config_validates_enums(tmp_path)`

**Purpose:** Implements `test_browser_config_validates_enums`.

**Steps:**

1. Return the computed result to the caller.

#### `def test_client_off_returns_unavailable()`

**Purpose:** Implements `test_client_off_returns_unavailable`.

**Steps:**

1. Return the computed result to the caller.

#### `def test_client_warm_dispatches_to_daemon()`

**Purpose:** Implements `test_client_warm_dispatches_to_daemon`.

**Steps:**

1. Return the computed result to the caller.

#### `def test_client_warm_unreachable_is_unavailable_not_crash()`

**Purpose:** Implements `test_client_warm_unreachable_is_unavailable_not_crash`.

**Steps:**

1. Return the computed result to the caller.

#### `def test_client_autostart_spawns_daemon_when_unreachable(monkeypatch)`

**Purpose:** Implements `test_client_autostart_spawns_daemon_when_unreachable`.

**Steps:**

1. Return the computed result to the caller.

#### `def test_client_no_autostart_when_disabled(monkeypatch)`

**Purpose:** Implements `test_client_no_autostart_when_disabled`.

**Steps:**

1. Return the computed result to the caller.

---

## Related

- [tests/_index](../_index/)
