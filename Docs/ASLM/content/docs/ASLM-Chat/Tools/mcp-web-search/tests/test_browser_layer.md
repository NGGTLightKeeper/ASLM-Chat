---
title: "test_browser_layer"
draft: false
---

## Module `test_browser_layer`

`Tools/mcp-web-search/tests/test_browser_layer.py` — ASLM Chat Python module.

---

## Classes

### `class _FakeResp`

**Purpose:** Type `_FakeResp` defined in `test_browser_layer.py`.

### `class _FakeHttp`

**Purpose:** Type `_FakeHttp` defined in `test_browser_layer.py`.

### `class _Boom`

**Purpose:** Type `_Boom` defined in `test_browser_layer.py`.

---

## Public functions

#### `def test_identity_checkpoint_and_latest_good(tmp_path)`

**Purpose:** test_identity_checkpoint_and_latest_good defined in test_browser_layer.py

**Steps:**

1. Return the computed result to the caller.

#### `def test_identity_prune_keeps_newest_good(tmp_path)`

**Purpose:** test_identity_prune_keeps_newest_good defined in test_browser_layer.py

**Steps:**

1. Return the computed result to the caller.

#### `def test_identity_rotate_burns_latest_and_falls_back(tmp_path)`

**Purpose:** test_identity_rotate_burns_latest_and_falls_back defined in test_browser_layer.py

**Steps:**

1. Return the computed result to the caller.

#### `def test_identity_rotate_with_no_fallback_returns_none(tmp_path)`

**Purpose:** test_identity_rotate_with_no_fallback_returns_none defined in test_browser_layer.py

**Steps:**

1. Return the computed result to the caller.

#### `def test_identity_cookie_header_is_host_scoped(tmp_path)`

**Purpose:** test_identity_cookie_header_is_host_scoped defined in test_browser_layer.py

**Steps:**

1. Return the computed result to the caller.

#### `def test_browser_section_defaults()`

**Purpose:** test_browser_section_defaults defined in test_browser_layer.py

**Steps:**

1. Return the computed result to the caller.

#### `def test_browser_config_validates_enums(tmp_path)`

**Purpose:** test_browser_config_validates_enums defined in test_browser_layer.py

**Steps:**

1. Return the computed result to the caller.
2. Parse or serialize JSON payloads.

#### `def test_client_off_returns_unavailable()`

**Purpose:** test_client_off_returns_unavailable defined in test_browser_layer.py

**Steps:**

1. Return the computed result to the caller.

#### `def test_client_warm_dispatches_to_daemon()`

**Purpose:** test_client_warm_dispatches_to_daemon defined in test_browser_layer.py

**Steps:**

1. Return the computed result to the caller.

#### `def test_client_warm_unreachable_is_unavailable_not_crash()`

**Purpose:** test_client_warm_unreachable_is_unavailable_not_crash defined in test_browser_layer.py

**Steps:**

1. Return the computed result to the caller.

---

## Private functions

#### `def _state(*cookies) -> dict`

**Purpose:** _state defined in test_browser_layer.py

**Steps:**

1. Return the computed result to the caller.
