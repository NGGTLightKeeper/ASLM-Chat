---
title: "test_onion_registry"
draft: false
---

## Module `test_onion_registry`

`Tools/mcp-web-search/tests/test_onion_registry.py` — ASLM Chat Python module.

---

## Classes

### `class _Resp`

**Purpose:** Implements `_Resp`.

#### `def _Resp.__init__(self, headers)`

**Purpose:** Implements `__init__`.

### `class _Fake`

**Purpose:** Implements `_Fake`.

#### `def _Fake.__init__(self, header_value)`

**Purpose:** Implements `__init__`.

#### `def _Fake.get(self, url, **kw)`

**Purpose:** Implements `get`.

---

## Public functions

#### `def test_seed_loads_vetted_services()`

**Purpose:** Implements `test_seed_loads_vetted_services`.

#### `def test_lookup_helpers()`

**Purpose:** Implements `test_lookup_helpers`.

#### `def test_resolver_prefers_fresh_and_caches(monkeypatch)`

**Purpose:** Implements `test_resolver_prefers_fresh_and_caches`.

#### `def test_resolver_falls_back_to_seed(monkeypatch)`

**Purpose:** Implements `test_resolver_falls_back_to_seed`.

#### `def test_resolver_force_refetches(monkeypatch)`

**Purpose:** Implements `test_resolver_force_refetches`.

---

## Related

- [tests/_index](../../_index/)
