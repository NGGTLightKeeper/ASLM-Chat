---
title: "test_mcp_bridge_contract"
draft: false
---

## Module `test_mcp_bridge_contract`

`Tools/mcp-browser-agent/tests/test_mcp_bridge_contract.py` — ASLM Chat Python module.

---

## Test methods

#### `def test_mcp_server_metadata_and_tool_surface(bridge_module) -> None`

**Purpose:** Implements `test_mcp_server_metadata_and_tool_surface` in `test_mcp_bridge_contract.py`.

**Steps:**

1. Iterate and transform or accumulate state.

#### `def test_supports_known_engines(bridge_module, engine) -> None`

**Purpose:** Implements `test_supports_known_engines` in `test_mcp_bridge_contract.py`.

#### `def test_flatten_content_joins_text_chunks(bridge_module) -> None`

**Purpose:** Implements `test_flatten_content_joins_text_chunks` in `test_mcp_bridge_contract.py`.

#### `def test_browser_keepalive_settings_normalizes_navigate_url(bridge_module) -> None`

**Purpose:** Implements `test_browser_keepalive_settings_normalizes_navigate_url` in `test_mcp_bridge_contract.py`.

#### `def test_call_tool_delegates_to_execute_browser_tool(bridge_module, monkeypatch) -> None`

**Purpose:** Implements `test_call_tool_delegates_to_execute_browser_tool` in `test_mcp_bridge_contract.py`.

#### `def test_execute_browser_tool_local_blocked_while_waiting(bridge_module, monkeypatch) -> None`

**Purpose:** Implements `test_execute_browser_tool_local_blocked_while_waiting` in `test_mcp_bridge_contract.py`.

---

## Related

- [tests/_index](../../../_index/)
