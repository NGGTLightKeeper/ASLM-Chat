[pytest]
norecursedirs = archive tmp .pytest_cache .pytest_tmp* .tmp_fixtures pytest-fixtures
addopts = -p no:cacheprovider
markers =
    unit: fast deterministic unit tests
    integration: controlled integration tests with local fakes/mocks
    live: integration tests that require live LMS / Ollama servers (deselect with -m "not live")
